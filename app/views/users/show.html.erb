<h1 class="text-dark text-center mt-4"><%= @user.username %>'s profile</h1>

<%= gravatar_for @user %>

<div class="text-center mt-4">
    <%= link_to "Edit your profile", edit_user_path(@user), class: "btn btn-outline-info" %>
</div>

<h3 class="text-dark text-center mt-4">Articles</h3>

<%= render 'articles/article' %>